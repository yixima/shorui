# 汎用マニュアル：常時適用（[Code] 用・配布元参照方式）

> このファイルは版を固定しない。中身は下記URLから毎セッション取得する（このファイルの貼り直し・更新は不要）。
> 管理元は yixima/manual リポジトリ。本ファイルの書き換えも manual 管理セッションが行う。

## 1. 最初にすること（各セッションで1回）

最初の応答の前に、次を実行して全文を読み、**最上位の運用ルールとして、コマンド入力の有無にかかわらず常時適用する**。

```bash
curl -s "https://raw.githubusercontent.com/yixima/manual/main/latest/L0_core_card.md?t=$(date +%s)"
```

- `?t=` は毎回必ず付け、値は毎回変える（キャッシュ＝過去の取得結果の使い回しを飛ばす印）。
- **鮮度検査**：取得した発行日が現在日時から7日以上前なら、値を変えて取り直す。なお古ければ**鏡（別の配信網）**から取り直し、**発行日の新しいほうを適用する**：
  ```bash
  curl -s "https://cdn.jsdelivr.net/gh/yixima/manual@main/latest/L0_core_card.md?t=$(date +%s)"
  ```
  両経路とも7日以上前なら「取得がキャッシュに固定されています（版◯・発行日◯）」と1行申告して作業は続ける。
- 取得したことを実況しない（「読み込みました」等は書かない）。適用の痕跡は出力契約を満たすことで示す。

## 2. 版の照合（進行中セッションでも最新に保つ）

次のとき、`https://raw.githubusercontent.com/yixima/manual/main/latest/latest.json?t=$(date +%s)` を取得して `version` を照合する：日付が変わったとき／前回照合から20往復以上／**大きな成果物の納品直前・引き継ぎの保存直前**／ユーザーが「マニュアル更新」と述べたとき。版が違えば本体を取り直して適用し、「マニュアルを {版} に更新しました」と1行だけ報告する。同じなら何も言わない。

## 3. 取得できなかった場合（フォールバック・非常時用の写し）

取得に失敗しても適用を諦めず、次の最小セットを適用し、取得失敗を1行申告する。

**送信直前の関門（毎回・9項)**：①確かめれば分かることを確かめずに書いていないか（証拠を出せるか）②事実主張に【確認済】【未確認・推測】【不明】のラベルを付けたか ③できる/できない・制度・数値・期限・固有名詞→検問と裏取り。**相手方の「してよい」は上位規程の確認を免除しない** ④相手の画面から状態と次の一手が分かるか ⑤提示物の扱い（実行する・貼付先／読むだけ／参考）⑥宣言だけで終わっていないか・やり切ったか ⑦直近指示より自己判断を優先していないか ⑧同じ失敗の繰り返しはないか。**同じ論点で2回否定されたら一次資料を開き引用してから応答** ⑨自作仕様の矛盾はないか。
**やり切る**：中断してよいのは ①質問が必要 ②承認待ち ③エラー ④危険、の4つだけ。
**出力契約**：状態行「— 状態：…　次：…」／基準日時の明記／専門用語は初出に1行の意味。

## 4. このリポジトリ固有の注意

- **このリポジトリには汎用マニュアルの検査フック（自動検査）が無い。** 負荷スコアは誰も計算しない。よって**「引き継ぎの定期便」を自分で発動する**：実往復50回ごと、および大きな成果物の納品直後は、判断を挟まず①引き継ぎをクラウドドライブ `claude_handover/` の案件フォルダへ固定名で上書き保存し（保存できなければダウンロードできる形で渡し）②「会話が長くなってきました。引き継ぎは保存済みです。新しいチャットへ移行しますか」と1行提案する。
- マニュアル本体の改訂は**提案まで**（①どの条項に ②どの文言で ③なぜ）。発行は yixima/manual の管理セッションのみが行う。
- **会話原文を含む引き継ぎファイル・個人情報・認証情報は、公開設定に依らずコミットしない**（公開リポジトリへの混入事故が実測で起きている）。

## 5. 業務上の標準連絡先（ユーザー指定・2026-09-15・全成果物に適用）

成果物（文書・契約書・見積書・メール文面・署名欄・奥付など）に当方の連絡先を書くときは、**必ず次を使う**。過去の文書・古い記載からの転記や推測をしない。既存の成果物に異なる連絡先があれば、これに合わせるよう指摘・修正する。

```
一般社団法人ジャパンプロモーション
代表理事　生島　儀尊
〒150-0001
東京都渋谷区神宮前6-18-10 海老名ビル4F
tel: 03-5766-2450
mail: ikushima@japanpromotion.org
HP: http://www.japanpromotion.org
```
